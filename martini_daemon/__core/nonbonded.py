# less special class than before
# should get type change notifs through flag_...
# passed as class type to simulation and then simulation instantiates it
# and calls add_force on it
#
# for now there can only be one, each should take the names "nonbonded" and
# "exclusions", since the input file format has no good way to support multiple
