[build-system]
requires = ["setuptools", "setuptools-scm", "cython", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "martini_daemon"
authors = []
description = ""
readme = "README.md"
requires-python = ">=3.10"
keywords = []
classifiers = []
dependencies = ["numpy", "openmm", "freud-analysis", "tqdm"]
dynamic = ["version"]

[project.optional-dependencies]
all = [
# Some helpers
  "molly@git+https://git.sr.ht/~ma3ke/molly#egg=molly&subdirectory=bindings/python",
# Test
  "pytest",
# Docs
  "sphinx",
  "sphinx-autoapi"
]
