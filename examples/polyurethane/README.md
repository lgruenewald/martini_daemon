A cross linked polyurethane polymerization system. This example system right
now is too small and too concentrated to be meaningful, but it is good for
testing the reaction template.

- `./run.py` to run
- `vmd -e vis.tcl` to visualize
